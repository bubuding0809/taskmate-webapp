import React, { useRef } from "react";
import { type NextPage } from "next";
import Head from "next/head";
import { signIn, signOut, useSession } from "next-auth/react";
import { animated } from "@react-spring/web";
import { Parallax, ParallaxLayer, IParallax } from "@react-spring/parallax";
import { ClassNames } from "@emotion/react";

// TO BE DONE BY: Jansonn
// This is the landing page for the app. It should be the first page that the user sees when they visit the site.
// The page can be accessed at http://localhost:3000/landing
const navigation = [
  { name: "About Us", href: "#" },
  { name: "Features", href: "#" },
  { name: "Contact", href: "#" },
  { name: "Log In", href: "#" },
];

const LandingPage: NextPage = () => {
  return (
    <>
      <Head>
        <title>Taskmate</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <header>
        <nav className="sticky top-0 bg-white">
          <div className="flex bg-white">
            <div>
              <img
                className="w-34 h-20 justify-center pl-4"
                src="./images/logo.webp"
                alt="logo"
              />
            </div>
            <div className="my-5 ml-auto mr-10 space-x-8 text-right">
              {navigation.map((item) => (
                <a
                  key={item.name}
                  href={item.href}
                  className="font-serif text-2xl font-medium hover:text-red-300"
                >
                  {item.name}
                </a>
              ))}
            </div>
          </div>
        </nav>
      </header>
      <Parallax pages={3}>
        <ParallaxLayer
          speed={0.25}
          offset={0}
          factor={1.2}
          style={{ backgroundImage: `url(${"./images/picture1.jpg"})` }}
          className="flex h-max justify-center text-lg"
        >
          <div className="ml-16 flex items-center">
            <div className="flex flex-col items-center">
              <h1 className="ml-9 mt-10  text-left text-8xl font-bold tracking-tight">
                <span className="flex text-black">New Generation</span>
                <span className="flex pb-3 text-[#e99b9b] sm:pb-5">
                  Task Manager
                </span>
              </h1>
              <div>
                <span className="text-3xl text-black">
                  The New All-In-One Project Management
                </span>
                <span className="flex text-3xl text-black">
                  Catered For Your Team
                </span>
              </div>
              <div>
                <button className="mt-4 rounded bg-[#e99b9b] py-2 px-4 text-2xl text-white hover:animate-bounce">
                  <a href="#">Try Now</a>
                </button>
                <button className="mt-4 ml-2 rounded bg-[#e2dada] py-2 px-4 text-2xl text-black">
                  <a href="#">Live Demo</a>
                </button>
              </div>
            </div>
            <img
              src="./images/paul-weaver-unsplash.jpeg"
              className="my-32 ml-20 h-72 w-auto duration-500 ease-in-out hover:scale-150"
            />
          </div>
        </ParallaxLayer>
        <ParallaxLayer
          offset={1}
          speed={0.1}
          style={{ backgroundImage: `url(${"./images/picture3.jpg"})` }}
          className="flex items-center justify-center"
        >
          <h1>page2</h1>
        </ParallaxLayer>
        <ParallaxLayer
          speed={0.1}
          offset={2}
          style={{ backgroundImage: `url(${"./images/picture2.jpg"})` }}
          className="flex items-center justify-center"
        >
          page 3
        </ParallaxLayer>
      </Parallax>
    </>
  );
};

export default LandingPage;
